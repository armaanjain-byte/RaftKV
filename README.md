# RaftKV
A distributed key-value store implemented from scratch in Go using the Raft consensus protocol, with protocol rules checked against Raft's Figure 2 explicitly, at-most-once client semantics with a specified session lifecycle, and a deterministic seeded fault-injection framework with defined workload coverage.
